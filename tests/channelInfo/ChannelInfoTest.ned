package org.mixim.tests.channelInfo;

import org.mixim.tests.TestObject;

// Test network for class ChannelInfo tests.
simple ChannelInfoTest extends TestObject
{
    @class(ChannelInfoTest);
    @isNetwork(true);
}
